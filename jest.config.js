/** @type {import('jest').Config} */
module.exports = {
  preset: 'ts-jest',
  testEnvironment: 'node',
  // If you want to customize transform further, you can do so, but ts-jest preset handles this automatically
};
